<?xml version="1.0" encoding="utf-8" ?>
<local:TestContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="using:Maui.Controls.Sample.Issues"
    x:Class="Maui.Controls.Sample.Issues.ItemSizingStrategyMeasureFirstItem"
    Title="ItemSizingStrategy MeasureFirstItem Test">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Control panel -->
        <StackLayout Grid.Row="0" Orientation="Horizontal" Padding="10">
            <Label Text="Strategy:" VerticalOptions="Center" />
            <Picker x:Name="strategyPicker" AutomationId="StrategyPicker" SelectedIndex="0">
                <Picker.Items>
                    <x:String>MeasureFirstItem</x:String>
                    <x:String>MeasureAllItems</x:String>
                </Picker.Items>
            </Picker>
            <Label Text="Layout:" VerticalOptions="Center" />
            <Picker x:Name="layoutPicker" AutomationId="LayoutPicker" SelectedIndex="0">
                <Picker.Items>
                    <x:String>VerticalList</x:String>
                    <x:String>HorizontalList</x:String>
                    <x:String>VerticalGrid</x:String>
                </Picker.Items>
            </Picker>
        </StackLayout>

        <!-- CollectionView -->
        <CollectionView x:Name="collectionView" 
                        Grid.Row="1"
                        AutomationId="TestCollectionView"
                        ItemSizingStrategy="MeasureFirstItem">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="5" />
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid BackgroundColor="LightBlue" Padding="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackLayout>
                            <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold" />
                            <Label Text="{Binding Description}" FontSize="12" />
                            <!-- First item has more content to test MeasureFirstItem behavior -->
                            <Label Text="{Binding ExtraContent}" 
                                   FontSize="10" 
                                   IsVisible="{Binding HasExtraContent}" />
                        </StackLayout>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</local:TestContentPage>